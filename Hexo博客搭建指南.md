# Hexo 静态博客搭建指南：从零开始

## 一、Hexo 是什么？

**Hexo** 是一个快速、简洁且高效的博客框架。
*   **原理**：它使用 Markdown（或其他渲染引擎）解析文章，在几秒内利用 Node.js 生成静态网页（HTML/CSS/JS）。
*   **优势**：
    *   **速度快**：生成静态页面，访问速度极快。
    *   **免费部署**：生成的静态文件可以托管在 GitHub Pages、Vercel 等免费平台上，不需要购买服务器。
    *   **生态丰富**：拥有海量的主题（Themes）和插件（Plugins）。

---

## 二、环境准备 (Prerequisites)

在开始之前，你的电脑需要安装以下两个核心工具：

1.  **Node.js** (Hexo 的运行环境)
    *   下载地址：[Node.js 官网](https://nodejs.org/) (推荐下载 LTS 长期支持版)
    *   验证：打开终端输入 `node -v` 和 `npm -v`，有版本号即成功。
2.  **Git** (版本控制与发布工具)
    *   下载地址：[Git 官网](https://git-scm.com/)
    *   验证：打开终端输入 `git --version`。

### 🛡️ 进阶：使用 NVM 管理 Node 版本 (强烈推荐)

如果你使用 **NVM (Node Version Manager)**，这是管理多个 Node 版本的最佳实践。

**1. 安装与切换**
Hexo 推荐使用 **LTS (长期支持版)**，比如 Node 20。

```bash
# 安装最新的 LTS 版本
nvm install lts
# 或者指定版本
nvm install 20.11.0

# 切换到该版本 (重要：每次打开新终端都要确认版本)
nvm use 20.11.0
```

**2. 避坑指南：全局包的“隐形陷阱”**
在 NVM 环境下，**全局安装的包 (`npm i -g`) 是绑定在当前 Node 版本下的**。
*   如果你在 Node 18 下安装了 `hexo-cli`，切换到 Node 20 后，输入 `hexo` 会报错“命令不存在”。
*   **安全建议**：
    *   **方法 A (锁定环境)**：始终固定使用一个 LTS 版本（如 Node 20）来维护博客。
    *   **方法 B (使用 npx)**：不安装全局 `hexo-cli`，而是直接用 `npx` 运行（**最安全，推荐**）。
        ```bash
        # 初始化时
        npx hexo-cli init my-blog
        
        # 之后的所有命令前加 npx (无需全局安装 hexo)
        npx hexo s
        npx hexo g
        ```

### ⚡ 提速技巧：设置 NPM 国内镜像

**痛点**：`npm install` 默认连接国外服务器，经常卡住不动或报错。
**诊断**：如果长时间转圈，可以加上 `--verbose` 参数查看进度：`npm install --verbose`。
**解决**：推荐临时或永久切换到淘宝镜像源。

```bash
# 方式一：临时使用（推荐，不影响其他项目）
npm install --registry=https://registry.npmmirror.com

# 方式二：永久配置
npm config set registry https://registry.npmmirror.com

# 验证配置
npm config get registry
```

---

## 三、从零搭建步骤 (Step-by-Step)

### 1. 安装 Hexo CLI
打开终端（Windows 推荐使用 PowerShell 或 Git Bash），输入以下命令全局安装 Hexo 脚手架：

```bash
# 推荐使用 npx 方式（无需全局安装，避免污染环境）
npx hexo-cli init my-blog
```

### 2. 初始化博客项目
如果你上面使用了 `npx` 命令，那么 `my-blog` 文件夹已经创建好了。

```bash
# 进入目录
cd my-blog

# 安装依赖（关键步骤：加入兼容参数，避免报错）
npm install --legacy-peer-deps --registry=https://registry.npmmirror.com
```

> **注意**：
> *   `--legacy-peer-deps`：解决 npm 7+ 带来的 `ERESOLVE` 依赖冲突报错。
> *   `--registry=...`：使用淘宝镜像，解决下载卡顿问题。

### 3. 本地预览
Hexo 内置了一个轻量级服务器，可以实时预览效果：

```bash
# 使用 npx 运行（推荐）
npx hexo server

# 或者简写为
npx hexo s
```
此时打开浏览器访问 `http://localhost:4000`，你应该能看到 Hexo 的默认页面了。

### 4. 写作与发布
#### 4.1 创建文章
使用以下命令创建一篇新文章：

```bash
npx hexo new "文章标题"
```
例如：`npx hexo new "Java多线程基础"`。
执行后，你会看到生成了一个 Markdown 文件，路径通常在 `source/_posts/文章标题.md`。

#### 4.2 编辑内容
找到刚才生成的 `.md` 文件，使用你喜欢的编辑器打开。
你会看到文件开头有类似这样的配置（Front-matter）：

```yaml
---
title: Java多线程基础
date: 2024-02-14 10:00:00
tags: 
---
```
在 `---` 下方即可开始正文写作。Hexo 支持标准的 Markdown 语法。

#### 4.3 常用写作技巧
**插入代码块**：
```java
public class MyThread extends Thread {
    public void run() {
        System.out.println("MyThread running");
    }
}
```

**插入图片**：
建议使用图床或者将图片放在 `source/images` 目录下，然后使用 `![](/images/图片名.png)` 引用。

#### 4.4 预览与部署
写完后，再次运行 `npx hexo s` 即可在本地看到更新。
确认无误后，推送到 GitHub（需配置好自动部署流程）。

### 5. 访问博客与域名配置
#### 5.1 查看博客网址
部署成功后，你的博客会自动上线，而不是直接展示源码。
1.  进入 GitHub 仓库页面。
2.  点击顶部菜单的 **Settings** -> 左侧边栏 **Pages**。
3.  在顶部你会看到：**"Your site is live at ..."**，点击那个链接即可访问。

通常默认地址是：`https://你的用户名.github.io/仓库名/`

#### 5.2 自定义域名（进阶）
如果你在 GitHub Pages 设置中绑定了自定义域名（如 `www.example.com`），**强烈建议**在本地也配置一下，否则下次部署可能会失效。

1.  在 `source` 目录下创建一个名为 `CNAME` 的文件（无后缀）。
2.  文件内容只需一行：你的域名（例如 `www.example.com`）。
3.  推送到 GitHub。

#### 5.3 修正配置文件（重要）
为了让 CSS 和图片正常显示，请修改根目录下的 `_config.yml`：

```yaml
# 替换为你的真实域名或 GitHub Pages 地址
url: https://你的用户名.github.io/仓库名
# 如果是自定义域名，root 通常是 /
root: /
# 如果没有自定义域名，root 应该是 /仓库名/
# root: /my-blog/
```
修改完记得重新部署。

#### 5.4 常见问题：仓库私有化 (Private Repo)
**Q：如果我把仓库设为 Private，别人还能看到博客吗？**
A：**不能（除非付费）**。
*   **GitHub Free 用户**：GitHub Pages 只能从 **Public** 仓库发布。如果仓库设为 Private，Pages 服务会停止。
*   **GitHub Pro/Team 用户**：可以从 Private 仓库发布 Pages 站点，并且可以选择站点是对外公开还是仅限团队访问。

**最佳实践**：
对于普通用户，建议仓库保持 **Public**。如果你想保护源码（Markdown原稿），可以采用双仓库策略：
1.  **私有仓库**：存放 Hexo 源码。
2.  **公开仓库**：仅存放 `public` 文件夹生成的静态网页（通过 Action 自动推送）。

### 6. 常用命令速查

| 命令 | 简写 | 作用 | 备注 |
| :--- | :--- | :--- | :--- |
| `hexo new "文章标题"` | `hexo n` | 新建文章 | 文章会在 `source/_posts/` 目录下 |
| `hexo clean` | 无 | 清除缓存 | 样式错乱时先运行这个 |
| `hexo generate` | `hexo g` | 生成静态文件 | 生成的文件在 `public/` 目录 |
| `hexo server` | `hexo s` | 启动本地服务器 | 默认端口 4000 |
| `hexo deploy` | `hexo d` | 部署到远程 | 需配置 `_config.yml` |

---

## 四、更换主题 (进阶)

Hexo 默认的主题叫 Landscape，如果你不喜欢，可以换成流行的主题，比如 **Butterfly** 或 **Next**。

以安装 **Next** 主题为例：

1.  **下载主题**：
    ```bash
    git clone https://github.com/next-theme/hexo-theme-next themes/next
    ```
2.  **启用主题**：
    打开根目录下的 `_config.yml` 文件，找到 `theme` 字段，修改为：
    ```yaml
    theme: next
    ```
3.  **预览**：
    ```bash
    hexo clean
    hexo s
    ```

---

## 五、自动部署到 GitHub Pages (结合 Actions)

> **💡 小知识**：`hexo d` 是 `hexo deploy` 的缩写。
> *   **传统痛点**：每次写完文章都要在本地运行 `hexo g -d`，依赖本地环境，容易报错。
> *   **现代方案**：利用 GitHub Actions，你只需要 `git push` 源码，云端自动构建并发布。

### 1. 准备 GitHub 仓库
*   新建一个仓库，名字最好是 `你的用户名.github.io`（这样访问地址就是 `https://你的用户名.github.io`）。
*   将本地的 `my-blog` 代码推送到这个仓库。

### 2. 创建 Workflow 文件
在项目根目录创建文件 `.github/workflows/deploy.yml`，并填入以下内容：

```yaml
name: Deploy Hexo Blog

# 触发条件：当 main 分支有代码推送时
on:
  push:
    branches:
      - main

# 权限设置：允许 Token 读写 Pages
permissions:
  contents: read
  pages: write
  id-token: write

# 任务配置
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest # 使用 GitHub 提供的最新 Ubuntu 虚拟机
    steps:
      # 1. 拉取代码
      - name: Checkout source
        uses: actions/checkout@v4

      # 2. 安装 Node.js (Hexo 依赖)
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # 3. 安装依赖 & 构建
      - name: Install & Build
        run: |
          npm install
          npx hexo generate

      # 4. 上传构建产物 (public 目录)
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public # Hexo 默认生成的静态文件目录

      # 5. 部署到 GitHub Pages
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
```

### 3. 开启 GitHub Pages
1.  进入 GitHub 仓库页面，点击 **Settings**。
2.  在左侧菜单找到 **Pages**。
3.  在 **Build and deployment** 部分：
    *   Source 选择 **GitHub Actions**。
    *   (注意：不需要选择分支，Actions 会自动处理)

### 4. 见证奇迹
*   修改一篇 Markdown 文章，或新建一篇文章。
*   执行 `git add .`, `git commit -m "new post"`, `git push`。
*   去仓库的 **Actions** 标签页，你会看到一个黄色的圆圈在转，变绿后说明发布成功。
*   访问你的博客网址，内容已更新！

---

## 六、常见问题 (Q&A)

### Q1: `hexo init` 报错 `exo : 无法将“exo”项识别为...`
*   **原因**：拼写错误，或者 `hexo-cli` 没有正确安装到全局变量中。
*   **解决**：
    1.  检查拼写，是 `hexo` 不是 `exo`。
    2.  推荐使用 `npx hexo init my-blog`，无需全局安装，更稳定。

### Q2: 安装时满屏黄字 `ERESOLVE overriding peer dependency`
*   **现象**：npm 提示依赖冲突，涉及 `eslint` 等插件。
*   **原因**：npm 7+ 对依赖版本检查非常严格，而部分 Hexo 老插件未及时更新适配。
*   **解决**：
    *   **方案 A (推荐)**：忽略警告，通常不影响使用。
    *   **方案 B**：使用兼容命令 `npm install --legacy-peer-deps`。

### Q3: 报错 `Cannot read properties of null (reading 'matches')`
*   **原因**：npm 缓存损坏或 `node_modules` 状态异常。
*   **解决**：
    ```bash
    # 1. 强制删除依赖文件夹
    Remove-Item -Recurse -Force node_modules
    Remove-Item -Force package-lock.json
    
    # 2. 清理缓存
    npm cache clean --force
    
    # 3. 重新安装 (带上兼容参数)
    npm install --legacy-peer-deps
    ```

### Q4: `npm install` 卡住不动
*   **原因**：连接 npm 官方源（国外）网络超时。
*   **解决**：切换淘宝镜像源。
    ```bash
    npm install --registry=https://registry.npmmirror.com
    ```
